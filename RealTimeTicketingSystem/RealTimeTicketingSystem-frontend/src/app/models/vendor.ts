// Interface representing vendor-related data
export interface Vendor {
  vendorId: string; // Unique identifier for the vendor
  totalTicketsReleased: number; // Total number of tickets released by the vendor
}
